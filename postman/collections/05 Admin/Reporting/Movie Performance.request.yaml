$kind: http-request
name: Movie Performance
url: "{{base_url}}/api/admin/reports/movies?startDate={{report_start_date}}&endDate={{report_end_date}}&currency={{report_currency}}&sort=REVENUE&direction=DESC&page=0&size=20"
method: GET
headers:
  Authorization: Bearer {{admin_token}}
order: 7000
