$kind: http-request
name: Get Now Showing Movies
description: |-
  Purpose: List movies currently marked as NOW_SHOWING.
  Authentication required: No.
  Notes: The demo movie uses NOW_SHOWING status.
url: "{{base_url}}/api/public/movies/now-showing"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code success", function () {
          pm.response.to.have.status(200);
      });

      const response = pm.response.json();

      pm.test("Response has success field", function () {
          pm.expect(response).to.have.property("success");
      });

      pm.test("Response has message field", function () {
          pm.expect(response).to.have.property("message");
      });

      pm.test("Response has data field", function () {
          pm.expect(response).to.have.property("data");
      });
    language: text/javascript
order: 3020
