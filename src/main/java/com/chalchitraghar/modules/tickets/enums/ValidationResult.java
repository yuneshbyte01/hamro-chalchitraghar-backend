package com.chalchitraghar.modules.tickets.enums;

public enum ValidationResult {
    SUCCESS,
    ALREADY_USED,
    REVOKED,
    EXPIRED,
    TOO_EARLY,
    TOO_LATE,
    INVALID,
    SHOW_CANCELLED,
    BOOKING_CANCELLED,
    SYSTEM_ERROR
}
